# ── Cloudflare Tunnel ────────────────────────────────────────────────────────
# Get from: dash.cloudflare.com → Zero Trust → Networks → Tunnels → your tunnel
CLOUDFLARE_TUNNEL_TOKEN=

# ── App URL ───────────────────────────────────────────────────────────────────
APP_BASE_URL=https://education.blastgroup.org
CHECKOUT_SUCCESS_URL=https://education.blastgroup.org/checkout/success
CHECKOUT_CANCEL_URL=https://education.blastgroup.org/checkout/cancelled
CHECKOUT_RETURN_URL=https://education.blastgroup.org/checkout/success

# ── Admin account (created on first start if DB is empty) ─────────────────────
INITIAL_ADMIN_EMAIL=admin@blastgroup.org
INITIAL_ADMIN_PASSWORD=

# ── Stripe ────────────────────────────────────────────────────────────────────
# Get from: dashboard.stripe.com → Developers → API keys
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
# Get from: dashboard.stripe.com → Developers → Webhooks → your endpoint → Signing secret
# Webhook URL to register: https://education.blastgroup.org/api/billing/stripe-webhook
STRIPE_WEBHOOK_SECRET=
# Get from: dashboard.stripe.com → Products → your product → Price ID
STRIPE_PRICE_ID=

# ── Encryption key ────────────────────────────────────────────────────────────
# Generate once with: python3 -c "import secrets; print(secrets.token_hex(32))"
# Keep this value forever — changing it invalidates stored CPF data.
CPF_ENCRYPTION_KEY=

# ── Email (Resend) ────────────────────────────────────────────────────────────
# Get from: resend.com → API Keys
RESEND_API_KEY=
RESEND_FROM_EMAIL=noreply@blastgroup.org
